substitutions:
  name: lvgl-kawaii-face
  friendly_name: LVGL_Kawaii_Face
  background_color: 0x000000
  text_color: 0xFFFFFF
  text_color_secondary: 0x767676

  micro_wake_word_model: okay_nabu




  allowed_characters: " []¿?¡!#%'()+,-—_./:µ³°ªº0123456789ABCDEFGHIJKLMNÑOPQRSTUVWXYZabcdefghijklmnñopqrstuvwxyzáéíóúÁÉÍÓÚäëïöüàâçèêîôùûÀÂÇÈÊÎÔÙÛ"
  font_glyphsets: "GF_Latin_Core"
  
  # for Greek use "Noto Sans" for other languages use a compatible font family
  font_family: Figtree  
  
  frigate: "http://192.168.1.38:8123/local/snapshots/last_snapshot.jpg"
  #frigate1: rtsp://Tapoone:Tapoone132@192.168.1.56:554/stream1
 
  media_player_entity: "media_player.waveshare_esp32_p4_media_player"
  weather_entity: weather.forecast_maison #weather

  icon_ha: https://aguacatec.es/wp-content/uploads/2026/04/icon_ha.png

  # Ken Burns animation parameters
  anim_duration_ms: "3000"   # Duration of one zoom+pan phase (ms)
  zoom_start: "256"          # 1.0x (Pas de zoom au début)
  zoom_end: "332"            # ~1.3x (Zoom maximum à l'arrivée         # ~1.4x (256 * 1.4 = 358)
  pan_range_x: "200"   # pixels to pan on X axis 160
  pan_range_y: "90"          # pixels to pan on Y axis 90
  
  power_icon:        "\U0000e909"
  play_icon:         "\U0000e90a"
  pause_icon:        "\U0000e90b"
  back_step_icon:    "\U0000e90c"
  forward_step_icon: "\U0000e90d"
  volume_off_icon:   "\U0000e90e"
  volume_on_icon:    "\U0000e90f"
  volume_minus_icon: "\U0000e910"
  volume_plus_icon:  "\U0000e911"
  repeat_all_icon:   "\U0000e912"
  repeat_off_icon:   "\U0000e913"
  repeat_one_icon:   "\U0000e914"
  ha:                "\U0000e90b" # home assistant
  ha_icon:           "\U000F07D0" # home assistant
  humidity:          "\U0000e912" # humidity
  humidity_icon:     "\U0000e938" 
  temperature:       "\U0000e915" # temperature
  temperature_icon:  "\U0000e939" 
  tvoc:              "\U0000e914" # air quality
  co2:               "\U0000e913" # co2
  door:              "\U000F081A"
  garage:            "\U000F06D9"
  shutter:           "\U000F1A8B"
  setting:           "\U000F08D6"
  alarm:             "\U000F0BC4"
  exit_icon:         "\U0000e902"
  recognition:       "\U000F0C7B"
  vlc:               "\U000F057C"
  youtube:           "\U000F05C3"
  application:       "\U000F0675"
  call:              "\U000F0919" 
 

  settings_icon:                 "\U0000e903"
  info_icon:                     "\U0000e904"
  devices_icon:                  "\U0000e905"
  home_icon:                     "\U0000e906"
  ceiling_icon:                  "\U0000e907"
  lightbulb_icon:                "\U0000e908"
  spotlight_group_icon:          "\U0000e915"
  desk_lamp_icon:                "\U0000e916"
  pendant_lamp_icon:             "\U0000e917" 
  ceiling_lamp_icon:             "\U0000e918"
  ceiling_lamp_variant_icon:     "\U0000e921"
  night_lamp_icon:               "\U0000e919"
  swipe_icon:                    "\U0000e91a"
  brightness_icon:               "\U0000e900"
 

  voice_assist_idle_phase_id: "1"
  voice_assist_listening_phase_id: "2"
  voice_assist_thinking_phase_id: "3"
  voice_assist_replying_phase_id: "4"
  voice_assist_not_ready_phase_id: "10"
  voice_assist_error_phase_id: "11"
  voice_assist_muted_phase_id: "12"
  voice_assist_timer_finished_phase_id: "20"

  loading_illustration_background_color: "000000"
  idle_illustration_background_color: "000000"
  listening_illustration_background_color: "FFFFFF"
  thinking_illustration_background_color: "FFFFFF"
  replying_illustration_background_color: "FFFFFF"
  error_illustration_background_color: "000000"
  timer_illustration_background_color: "FFFFFF"
  
  alarm_panel_entity:   "alarm_control_panel.alarmo" #"alarm_control_panel.security"
  pin_code:             "1169"

  unlock_pin_code: "1169" 

  shield_home_icon:      "\U000F068A" # home
  shield_away_icon:      "\U000F099D" # away
  shield_night_icon:     "\U000F1828" # night
  shield_vacation_icon:  "\U000F06BB" # vacation
  shield_disarm_icon:    "\U000F099E" # disarmed
  shield_arming_icon:    "\U000F0498" # arming
  micro_sd:              "\U000F07DC"

esphome:
  name: lvgl-kawaii-face
  friendly_name: lvgl-kawaii-face
  #includes:
    #- esp_wifi_tune.h 
  on_boot:
    priority: 850
    then:
      - logger.log: "Démarrage - Attente du montage de la carte SD..."
      - delay: 5s
      - logger.log: "Délai écoulé - Allumage du rétroéclairage"
      - light.turn_on: backlight
      # Démarrer le flux vidéo. Indispensable : face_detection attend le flux
      # mais ne le lance pas (if (!camera_->is_streaming()) return;), et les
      # dimensions de l'image ne sont renseignées qu'une fois le flux actif —
      # d'où le "image 0x0" tant que personne ne l'a démarré.
      - esp_cam_sensor.start_streaming:
          id: tab5_cam
      - delay: 500ms
      # État caméra : si l'image est encore 0x0 ici, inutile de chercher plus
      # loin, la détection ne verra jamais rien.
      - lambda: |-
          ESP_LOGI("kawaii.follow", "caméra au boot : %dx%d",
                   id(tab5_cam).get_image_width(), id(tab5_cam).get_image_height());



esp32:
  #variant: esp32p4
  flash_size: 32MB
  board: esp32-p4-evboard
  cpu_frequency: 360MHz
  engineering_sample: true
  framework:
    type: esp-idf
    #components:
      #- espressif/esp-dsp==1.8.0
    advanced:
      enable_idf_experimental_features: true
      disable_vfs_support_dir: false
      #loop_task_stack_size: "16384" #"32768"
      #disable_fatfs: False
      include_builtin_idf_components:
        - fatfs
        - wear_levelling
    sdkconfig_options:

      CONFIG_COMPILER_OPTIMIZATION_PERF: y        # perf plutôt que taille
      #CONFIG_CACHE_L2_CACHE_128KB: y              # + de cache L2
      CONFIG_SPIRAM_XIP_FROM_PSRAM: y             # soulage le bus flash
      #CONFIG_LCD_DSI_ISR_IRAM_SAFE: y             # pas de glitch DSI
      CONFIG_GDMA_ISR_IRAM_SAFE: y    
      
      CONFIG_ESP_HOSTED_MEMPOOL_PREFER_SPIRAM: "y"
      CONFIG_ESP_TASK_WDT_EN: "10"

      CONFIG_SPIRAM_FETCH_INSTRUCTIONS: "n"
      CONFIG_SPIRAM_RODATA: "n"
      CONFIG_ESP32_SPIRAM_SUPPORT: "y"
      CONFIG_SPIRAM_USE_MALLOC: "y"
      CONFIG_FATFS_LFN_STACK: "y"
      CONFIG_LWIP_SO_RCVBUF: "y"
      CONFIG_LWIP_MAX_SOCKETS: "16"
      CONFIG_VFS_SUPPORT_IO: "y"
      CONFIG_VFS_MAX_COUNT: "16" 
      CONFIG_ESP_MAIN_TASK_STACK_SIZE: "16384"
      CONFIG_ESP_TIMER_TASK_STACK_SIZE: "4096"
      #CONFIG_ESP_TASK_WDT_TIMEOUT_S: "60"
      CONFIG_ESP_TASK_LOOP_STACK_SIZE: "16384"
      CONFIG_FATFS_LFN_HEAP: "y"
      #DCONFIG_USE_AUDIO_AAC_SUPPORT: "y"
      DCONFIG_ESP_JPG_DECODE_ENABLE: "y"
      DCONFIG_ESPHOME_ENABLE_PNGLE: "1"
      CONFIG_SPIRAM_SPEED_200M: "y"  # ← NOUVEAU: 200 MHz au lieu de défaut
      CONFIG_SPIRAM_SPEED: "200"

      # Buffers WiFi distant (C6) + agrégation AMPDU = gros gain d'efficacité
      CONFIG_WIFI_RMT_STATIC_RX_BUFFER_NUM: "16"
      CONFIG_WIFI_RMT_DYNAMIC_RX_BUFFER_NUM: "64"
      CONFIG_WIFI_RMT_DYNAMIC_TX_BUFFER_NUM: "64"
      CONFIG_WIFI_RMT_AMPDU_TX_ENABLED: "y"
      CONFIG_WIFI_RMT_TX_BA_WIN: "32"
      CONFIG_WIFI_RMT_AMPDU_RX_ENABLED: "y"
      CONFIG_WIFI_RMT_RX_BA_WIN: "32"
      ## Buffers lwIP (UDP surtout pour toi)
      CONFIG_LWIP_UDP_RECVMBOX_SIZE: "64"
      CONFIG_LWIP_TCPIP_RECVMBOX_SIZE: "64"
      CONFIG_LWIP_TCP_RECVMBOX_SIZE: "64"
      CONFIG_LWIP_TCP_SND_BUF_DEFAULT: "65534"
      CONFIG_LWIP_TCP_WND_DEFAULT: "65534"
      CONFIG_LWIP_TCP_SACK_OUT: "y"   

logger:

api:
  encryption:
    key: "eA3unaomyBRTnYqkUigjfPJr2nqlMwXzox9XmAQ5Qis="

ota:
  - platform: esphome
psram:
  mode: hex
  speed: 200MHz
  
wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password
  ap:
    ssid: LVGL_Kawaii_Fac Fallback Hotspot
    password: "lXNJRn4vERfL"

captive_portal:

http_request:

online_image:
  - url: "${frigate}"
    format: JPG
    id: cctv_img
    byte_order: little_endian    
    type: RGB565
    resize: 1000x600 #450x300 #1000x700

external_components:
  - source:
      type: git
      url: https://github.com/youkorr/esphome-lvgl-kawaii
      ref: claude/lvgl-kawaii-face-bugs-j225qm
    components: [lvgl_kawaii_face]
    refresh: 0s

  - source:
      type: git
      url: https://github.com/youkorr/lvgl_9.5
      # lvgl_9.5 n'a besoin d'aucune modification pour le visage kawaii : on
      # reste sur la branche existante. (Il n'y a PAS de branche
      # claude/lvgl-kawaii-face-bugs-j225qm sur ce dépôt-là.)
      ref: claude/jolly-lamport-ERZOC #claude/mit-license-face-to-face-p9fc6m  #lvgl-ppa-msync-fix #6dc5b02f3e6940e782d12fd6748154c0c384272d #claude/fix-lottie-esp32-crash-K7tM1 #claude/fix-lottie-esp32-crashes-8gqrJ #claude/fix-lottie-esp32-crash-vVG5Y #claude/investigate-pr-conflicts-jyqoy #claude/add-lvgl-example-GJsCN #claude/create-sphere-BVofQ # claude/home-assistant-integration-6apll
    components: [lvgl, image, font]
    refresh: 0s

  # --- Caméra + détection de visage (pour le suivi du regard) ---------------
  # Retire ces deux sources ET les blocs esp_video/esp_cam_sensor/
  # face_detection plus bas si tu ne veux pas le suivi.
  - source:
      type: git
      url: https://github.com/youkorr/esphome_esp-video
      ref: claude/usb-uvc-consumer-integration-gnbflf
    components: [esp_video, esp_cam_sensor, esp_ipa, esp_sccb_intf]
    refresh: 0s

  # esp-dl / human_face_detect / human_face_recognition sont dans ce dépôt mais
  # ne sont PAS des composants ESPHome : face_detection/__init__.py les
  # enregistre lui-même comme composants ESP-IDF. Ne les liste pas ici.
  - source:
      type: git
      url: https://github.com/youkorr/face_detection
      ref: claude/esphome-face-detection-mit-71f1lr
    components: [face_detection]
    refresh: 0s

esp32_hosted:
  variant: ESP32C6
  reset_pin: GPIO54
  cmd_pin: GPIO19
  clk_pin: GPIO18
  d0_pin: GPIO14
  d1_pin: GPIO15
  d2_pin: GPIO16
  d3_pin: GPIO17
  active_high: true
  use_psram: true
  slot: 1

i2c:
  - id: bsp_bus
    sda: GPIO07
    scl: GPIO08
    frequency: 400kHz 

esp_ldo:
  - voltage: 2.5V
    channel: 3
  - voltage: 2.5V
    channel: 4  
    
output:
  - platform: ledc
    pin: GPIO32
    id: backlight_pwm
    frequency: 1000Hz
    inverted: true

light:
  - platform: monochromatic
    output: backlight_pwm
    name: "Display Backlight"
    id: backlight
    restore_mode: RESTORE_DEFAULT_ON
    default_transition_length: 250ms
# =============================================================================
# I2S AUDIO CONFIGURATION (ES7210 + ES8311)
# =============================================================================
i2s_audio:
  - id: audio_bus
    i2s_mclk_pin: GPIO13
    i2s_lrclk_pin: GPIO10
    i2s_bclk_pin: GPIO12

audio_dac:
  - platform: es8311
    id: es8311_dac
    i2c_id: bsp_bus
    address: 0x18

audio_adc:
  - platform: es7210
    id: es7210_adc
    i2c_id: bsp_bus
    address: 0x40

microphone:
  - platform: i2s_audio
    id: esp32_microphone
    i2s_audio_id: audio_bus
    i2s_din_pin: GPIO11
    sample_rate: 16000
    bits_per_sample: 16bit
    adc_type: external

speaker:
  - platform: i2s_audio
    id: esp32_speaker
    i2s_audio_id: audio_bus
    i2s_dout_pin: GPIO9
    audio_dac: es8311_dac
    dac_type: external
    channel: mono
    buffer_duration: 500ms
    bits_per_sample: 16bit
    sample_rate: 48000

  - platform: mixer
    id: mixing_speaker
    output_speaker: esp32_speaker
    source_speakers:
      - id: announcement_mixing_input
        timeout: never
      - id: media_mixing_input
        timeout: never

  - platform: resampler
    id: announcement_resampling_speaker
    output_speaker: announcement_mixing_input
    sample_rate: 48000
    bits_per_sample: 16

  - platform: resampler
    id: media_resampling_speaker
    output_speaker: media_mixing_input
    sample_rate: 48000
    bits_per_sample: 16



media_player:
  - platform: speaker
    name: "ESP32 P4 Media Player"
    id: speaker_media_player
    volume_min: 0.5
    volume_max: 0.75
    volume_increment: 0.05
    announcement_pipeline:
      speaker: announcement_resampling_speaker
      format: FLAC
      sample_rate: 48000
      num_channels: 1
    media_pipeline:
      speaker: media_resampling_speaker
      format: FLAC
      num_channels: 1
      sample_rate: 48000
    on_announcement:
      - if:
          condition:
            - microphone.is_capturing:
          then:
            - micro_wake_word.stop
            - voice_assistant.stop
      - mixer_speaker.apply_ducking:
          id: media_mixing_input
          decibel_reduction: 20
          duration: 0.0s
      - switch.turn_on: audio_amplifier
      - delay: 100ms
    on_state:
      if:
        condition:
          and:
            - not:
                voice_assistant.is_running:
            - not:
                media_player.is_announcing:
        then:
          - mixer_speaker.apply_ducking:
              id: media_mixing_input
              decibel_reduction: 0
              duration: 1.0s
    on_play:
      - logger.log: "Media player started playing, disabling microphone"
      - switch.turn_on: audio_amplifier
      - delay: 100ms
      - micro_wake_word.stop
      - voice_assistant.stop
    on_idle:
      - delay: 100ms
      - switch.turn_off: audio_amplifier
      - if:
          condition:
            not:
              voice_assistant.is_running:
          then:
            - micro_wake_word.start



micro_wake_word:
  id: mww
  models:
    - okay_nabu
#    - hey_mycroft
#    - hey_jarvis
  on_wake_word_detected:
    - component.update: main_display
    # Le mot d'éveil amène le visage à l'écran et le réveille.
    - lvgl.page.show:
        id: page_face
        animation: MOVE_LEFT
        time: 250ms
    - lvgl_kawaii_face.set_emotion:
        id: face
        emotion: excited
    - lambda: id(va_busy) = true;
    - voice_assistant.start:
        wake_word: !lambda return wake_word;

voice_assistant:
  microphone: esp32_microphone
  media_player: speaker_media_player
  micro_wake_word: mww
  noise_suppression_level: 2
  auto_gain: 31dBFS
  volume_multiplier: 2.0
  conversation_timeout: 300s

  on_start:
    - component.update: main_display

  on_listening:
    - component.update: main_display
    - lvgl_kawaii_face.set_emotion:
        id: face
        emotion: listening

  on_stt_end:
    - component.update: main_display
    - lvgl_kawaii_face.set_emotion:
        id: face
        emotion: thinking

  # `x` = le texte prononcé : le visage suit le CONTENU de la réponse,
  # et la bouche s'anime tant qu'il parle.
  on_tts_start:
    - component.update: main_display
    - lvgl_kawaii_face.set_emotion_from_text:
        id: face
        text: !lambda return x;
    - lvgl_kawaii_face.set_talking: true

  on_tts_end:
    - component.update: main_display
    - lvgl_kawaii_face.set_talking: false


  # --- End of flow → idle ---
  on_end:
    - wait_until:
        condition:
          - media_player.is_announcing:
        timeout: 0.5s
    - wait_until:
        - and:
            - not:
                media_player.is_announcing:
            - not:
                media_player.is_playing:
    - component.update: main_display
    - lvgl_kawaii_face.set_emotion:
        id: face
        emotion: neutral
    - lvgl_kawaii_face.set_talking: false
    - lambda: |-
        id(va_busy) = false;
        id(last_interaction) = millis();

    - micro_wake_word.start:

  on_error:
    - component.update: main_display
    - lvgl_kawaii_face.set_talking: false
    - lvgl_kawaii_face.set_emotion:
        id: face
        emotion: error
    - lambda: id(va_busy) = false;


  on_client_connected:
    - micro_wake_word.start:
  on_client_disconnected:
    - micro_wake_word.stop:

globals:
  # Index courant dans la liste des 17 expressions (démo).
  - id: demo_emotion_idx
    type: int
    restore_value: false
    initial_value: '0'
  - id: demo_emotion_name
    type: std::string
    restore_value: false
  # Vrai pendant toute une conversation : la vie autonome se tait alors.
  - id: va_busy
    type: bool
    restore_value: false
    initial_value: 'false'
  # Horodatage de la dernière sollicitation, pour l'endormissement.
  - id: last_interaction
    type: uint32_t
    restore_value: false
    initial_value: '0'
  - id: idle_emotion
    type: std::string
    restore_value: false
  # Diagnostic du suivi (affiché par le bandeau de debug).
  - id: dbg_faces
    type: int
    restore_value: false
    initial_value: '0'
  - id: dbg_gx
    type: int
    restore_value: false
    initial_value: '0'
  - id: dbg_gy
    type: int
    restore_value: false
    initial_value: '0'
  # Dernière fois qu'un visage a été vu par la caméra.
  - id: last_face_seen
    type: uint32_t
    restore_value: false
    initial_value: '0'

switch:


  - platform: gpio
    pin: GPIO53
    name: "Audio Amplifier"
    id: audio_amplifier
    restore_mode: ALWAYS_OFF

  # Fait défiler les 17 expressions, comme examples/basic_cycle du dépôt
  # d'origine. Sert à juger le rendu sans déclencher l'assistant vocal.
  # Coupe-le pour laisser le voice assistant piloter le visage.
  # Affiche le bandeau vert de diagnostic du suivi caméra sur la page du
  # visage. À couper une fois le réglage terminé.
  - platform: template
    name: "Debug suivi caméra"
    id: cam_debug_switch
    optimistic: true
    restore_mode: ALWAYS_OFF
    turn_off_action:
      - lvgl.label.update:
          id: cam_debug
          text: ""

  - platform: template
    name: "Démo expressions du visage"
    id: face_demo
    optimistic: true
    restore_mode: ALWAYS_OFF
    turn_off_action:
      - lvgl.label.update:
          id: face_caption
          text: ""

interval:
  # Bandeau de diagnostic du suivi caméra (5 Hz), seulement quand demandé.
  - interval: 200ms
    then:
      - if:
          condition:
            switch.is_on: cam_debug_switch
          then:
            - lvgl.label.update:
                id: cam_debug
                text: !lambda |-
                  char buf[64];
                  uint32_t age = id(last_face_seen) ? (millis() - id(last_face_seen)) : 99999;
                  snprintf(buf, sizeof(buf), "visages:%d  regard:%d,%d  vu il y a %ums",
                           id(dbg_faces), id(dbg_gx), id(dbg_gy), (unsigned) age);
                  return std::string(buf);

  # 2500 ms = HOLD_MS de l'exemple upstream.
  - interval: 2500ms
    then:
      - if:
          condition:
            switch.is_on: face_demo
          then:
            - lambda: |-
                // Même ordre que ALL_EMOTIONS[] dans examples/basic_cycle/main/main.c
                static const char *const EMOTIONS[] = {
                    "neutral", "happy", "worried", "wink", "love", "surprised",
                    "playful", "silly", "smirk", "working_hard", "excited",
                    "confused", "cool", "sleepy", "sad", "cry", "angry"};
                const int count = sizeof(EMOTIONS) / sizeof(EMOTIONS[0]);
                int i = id(demo_emotion_idx);
                id(demo_emotion_name) = EMOTIONS[i];
                id(demo_emotion_idx) = (i + 1) % count;
            - lvgl_kawaii_face.set_emotion:
                id: face
                emotion: !lambda return id(demo_emotion_name);
            - lvgl.label.update:
                id: face_caption
                text: !lambda return id(demo_emotion_name);

  - interval: 10s
    id: idle_life
    then:
      - if:
          condition:
            - lambda: 'return !id(va_busy) && !id(face_demo).state;'
          then:
            - lambda: |-
                // Palette complète (tempérament expressif). Retire des entrées
                // pour un visage plus discret.
                static const char *const IDLE_SET[] = {
                    "neutral", "happy", "cool", "smirk", "worried", "confused",
                    "silly", "playful", "excited", "wink", "sleepy"};
                const int n = sizeof(IDLE_SET) / sizeof(IDLE_SET[0]);

                // Endormissement : après 5 min sans sollicitation, le visage
                // pique du nez au lieu de continuer à s'agiter.
                uint32_t idle_ms = millis() - id(last_interaction);
                if (idle_ms > 300000) {
                  id(idle_emotion) = "sleepy";
                  return;
                }

                // Tirage sans répétition immédiate.
                static int prev = -1;
                int i = random_uint32() % n;
                if (i == prev) i = (i + 1) % n;
                prev = i;
                id(idle_emotion) = IDLE_SET[i];
            - lvgl_kawaii_face.set_emotion:
                id: face
                emotion: !lambda return id(idle_emotion);

  # ---------------------------------------------------------------------
  # SUIVI DU REGARD : les yeux se posent sur la personne vue par la caméra.
  # 10 Hz — assez fluide pour paraître vivant, assez lent pour ne rien coûter.
  # La détection tourne à son propre rythme (detection_interval) de son côté.
  # ---------------------------------------------------------------------
  - interval: 100ms
    then:
      - lambda: |-
          static const char *const TAG = "kawaii.follow";
          static bool had_face = false;
          static uint32_t last_log = 0;

          // Journalisation détaillée seulement quand l'interrupteur « Debug
          // suivi caméra » est armé, et à 2 Hz : à 10 Hz ça noierait le log.
          const bool dbg = id(cam_debug_switch).state;
          const bool log_now = dbg && (millis() - last_log > 500);

          int iw = id(tab5_cam).get_image_width();
          int ih = id(tab5_cam).get_image_height();

          auto faces = id(face_detect).get_detected_faces();
          if (faces.empty()) {
            if (had_face) {
              ESP_LOGI(TAG, "visage PERDU");
              had_face = false;
            }
            if (log_now) {
              last_log = millis();
              // Si l'image est 0x0, c'est la caméra qui ne fournit rien —
              // pas la détection qui échoue. Les deux se confondraient sinon.
              ESP_LOGI(TAG, "0 visage | image %dx%d %s", iw, ih,
                       (iw <= 0 || ih <= 0) ? "<- CAMERA MUETTE" : "");
            }
            id(dbg_faces) = 0;
            return;
          }

          // Le plus grand visage = la personne la plus proche. C'est elle
          // qu'on regarde, plutôt que de loucher entre deux personnes.
          const auto *best = &faces[0];
          int best_area = 0;
          for (auto &f : faces) {
            int area = (f.x2 - f.x1) * (f.y2 - f.y1);
            if (area > best_area) { best_area = area; best = &f; }
          }

          if (iw <= 0 || ih <= 0) {
            if (log_now) {
              last_log = millis();
              ESP_LOGW(TAG, "%d visage(s) mais image %dx%d : dimensions invalides",
                       (int) faces.size(), iw, ih);
            }
            return;
          }

          int cx = (best->x1 + best->x2) / 2;
          int cy = (best->y1 + best->y2) / 2;
          int gx = (cx * 200) / iw - 100;      // -100..100
          int gy = (cy * 200) / ih - 100;

          // La caméra te fait face : ce qu'elle voit à sa droite est à ta
          // gauche. On inverse X pour que le visage te regarde VRAIMENT.
          // Si les yeux partent à l'inverse de toi, retire cette ligne.
          gx = -gx;

          // Lissage : sans ça les yeux sautent sur chaque détection bruitée.
          static int sx = 0, sy = 0;
          sx = (sx * 2 + gx) / 3;
          sy = (sy * 2 + gy) / 3;

          if (!had_face) {
            ESP_LOGI(TAG, "visage ACQUIS (score %.2f)", best->score);
            had_face = true;
          }
          if (log_now) {
            last_log = millis();
            ESP_LOGI(TAG,
                     "%d visage(s) | boite (%d,%d)-(%d,%d) score %.2f | image %dx%d"
                     " | centre (%d,%d) -> brut (%d,%d) -> lisse (%d,%d)",
                     (int) faces.size(), best->x1, best->y1, best->x2, best->y2,
                     best->score, iw, ih, cx, cy, gx, gy, sx, sy / 2);
          }

          id(last_face_seen) = millis();
          id(last_interaction) = millis();     // vu quelqu'un = pas endormi
          id(face).look_at(sx, sy / 2, 1500);  // Y atténué : moins de roulis

          // Pour le bandeau de diagnostic (bouton « Debug suivi caméra »).
          id(dbg_faces) = faces.size();
          id(dbg_gx) = sx;
          id(dbg_gy) = sy / 2;

  # Plus personne depuis 10 s -> le visage se désintéresse.
  - interval: 2s
    then:
      - if:
          condition:
            - lambda: 'return id(last_face_seen) != 0 && (millis() - id(last_face_seen)) > 10000;'
          then:
            - lambda: id(last_face_seen) = 0;

  # Regard vagabond : les yeux balaient la pièce entre deux expressions.
  # Se tait dès qu'un visage est suivi par la caméra, sinon il tirerait les
  # yeux au hasard toutes les 2,5 s en pleine poursuite.
  - interval: 2500ms
    then:
      - if:
          condition:
            - lambda: |-
                return !id(va_busy) && !id(face_demo).state &&
                       (id(last_face_seen) == 0 || (millis() - id(last_face_seen)) > 3000);
          then:
            - lvgl_kawaii_face.look_at:
                id: face
                x: !lambda 'return (int) (random_uint32() % 161) - 80;'
                y: !lambda 'return (int) (random_uint32() % 81) - 40;'
                hold: 2s



touchscreen:
  - platform: gt911
    id: touch
    i2c_id: bsp_bus
    reset_pin: GPIO23
    update_interval: 8ms   # polling (pas d'interruption)
    on_touch:
      - lambda: |-
          ESP_LOGD("touchscreen", "Touch detected at (%d, %d)", touch.x, touch.y);
      - light.turn_on: backlight 
               
    transform:
      swap_xy: false
      mirror_x: true
      mirror_y: true   
    
display:
  - platform: mipi_dsi
    id: main_display
    show_test_card: false
    model: CUSTOM
    update_interval: never
    auto_clear_enabled: false
    hsync_back_porch: 160
    lane_bit_rate: 1000Mbps
    color_order: rgb
    lanes: 2
    reset_pin:
      number: 33
    hsync_pulse_width: 70
    hsync_front_porch: 160
    vsync_back_porch: 23
    vsync_pulse_width: 1
    vsync_front_porch: 12
    pclk_frequency: 51MHz 
    dimensions:
      width: 1024
      height: 600     
    init_sequence:   
      - [0xB2, 0x10]
      - [0x80, 0x8B]
      - [0x81, 0x78]
      - [0x82, 0x84]
      - [0x83, 0x88]
      - [0x84, 0xA8]
      - [0x85, 0xE3]
      - [0x86, 0x88]
      - [0x11, 0x00]
      - delay 120ms
      - [0x29]

# =============================================================================
# CAMÉRA + DÉTECTION DE VISAGE — le regard suit la personne
# =============================================================================
esp_video:
  i2c_id: bsp_bus
  xclk_pin: GPIO36
  xclk_freq: 24000000
  enable_jpeg: true
  enable_isp: true
  use_heap_allocator: false

esp_cam_sensor:
  id: tab5_cam
  i2c_id: bsp_bus
  sensor_type: ov5647
  # 640x480 en RGB565, pas 1280x960 en JPEG :
  #  - face_detection lit les pixels BRUTS (dl::image DL_IMAGE_PIX_TYPE_RGB565).
  #    En JPEG il analyserait des octets compressés, donc jamais de visage.
  #  - esp-dl tourne sur CPU (le P4 n'a pas de NPU) : 640x480 suffit largement
  #    pour repérer un visage à portée de voix, et coûte 4x moins qu'en 1280x960.
  pixel_format: RGB565
  resolution: "640x480"
  framerate: 15
  jpeg_quality: 15
  mirror_x: false
  mirror_y: false
  rotation: 0
  crop_offset_x: 0

face_detection:
  id: face_detect
  camera_id: tab5_cam
  # Pas de canvas_id ni de draw_enabled : on ne montre pas la caméra, on se
  # sert uniquement des coordonnées. Rien à dessiner, donc rien à ralentir.
  draw_enabled: false
  # face_recognition est le modèle par défaut ; la reconnaissance elle-même
  # reste désactivée (recognition_enabled: false par défaut), car le suivi du
  # regard n'a besoin que des coordonnées. Active-la si ta carte SD est montée
  # et ajoute face_db_path — voir le bloc commenté en fin de fichier.
  model_type: face_recognition
  score_threshold: 0.3
  nms_threshold: 0.5
  # 1 image sur 5 : le P4 n'a pas de NPU, esp-dl tourne sur CPU. Largement
  # assez réactif pour un regard, et ça laisse le CPU à LVGL.
  detection_interval: 5


lvgl:
  use_ppa: true
  use_ppa_img: true
  color_depth: 16
  buffer_size: 100%
  byte_order: little_endian
  fps_benchmark: true
  perf_monitor: true
  rotation: 180

  # Deux pages, et le visage est sur la DEUXIÈME : c'est exactement le cas qui
  # ne marchait pas (le visage se créait toujours sur la page affichée au
  # démarrage). Au boot on doit voir « Page 1 », pas le visage.
  pages:
    - id: page_home
      bg_color: ${background_color}
      widgets:
        - label:
            id: home_title
            text: "Page 1 - accueil"
            text_color: ${text_color}
            text_font: montserrat_38
            align: TOP_MID
            y: 60

        - label:
            id: home_hint
            text: "Le visage est sur la page 2 (dis « okay nabu » ou appuie ci-dessous)"
            text_color: ${text_color_secondary}
            text_font: montserrat_20
            align: TOP_MID
            y: 130

        - button:
            id: btn_show_face
            width: 320
            height: 90
            align: CENTER
            bg_color: 0x1F6FEB
            radius: 45
            widgets:
              - label:
                  text: "Voir le visage"
                  text_color: 0xFFFFFF
                  text_font: montserrat_28
                  align: CENTER
            on_click:
              - lvgl.page.show:
                  id: page_face
                  animation: MOVE_LEFT
                  time: 250ms

    - id: page_face
      bg_color: ${background_color}
      widgets:
        # Le conteneur du visage. Reprend à l'identique le panneau de
        # l'exemple upstream (0015/lvgl_kawaii_face, examples/basic_cycle) :
        #   set_size / center / bg_opa TRANSP / border_width 0 / pad_all 0
        #   / clear_flag(SCROLLABLE)
        # scrollable: false est indispensable — les canvas des yeux débordent
        # un peu du conteneur, donc sans ça un glissement du doigt sur le
        # visage le ferait défiler.
        #
        # ---> C'EST ICI QU'ON RÈGLE LA TAILLE DU VISAGE. <---
        # Les proportions suivent la taille (référence upstream : 135).
        #
        # Fond transparent : le noir de la page traverse, et le composant
        # reprend cette couleur pour les canvas des yeux/bouche. Le visage se
        # pose donc directement sur l'écran sombre, comme dans le GIF.
        # Le cadre arrondi du GIF de démo (make_demo_gif.py : rounded_rectangle
        # inset 8, radius 26, width 2, couleur 28/35/49 sur un cadre de 300).
        # Transposé ici : radius 36, bord 3, même couleur. Purement décoratif —
        # enlève ce bloc et remets face_panel directement dans la page si tu
        # n'en veux pas.
        #
        # Sa taille compte : dans le GIF les yeux occupent 55 % du cadre, alors
        # que le composant les dessine sur 90 % de face_sz. Le cadre doit donc
        # être nettement plus grand que le visage — ratio ~420/256, mesuré sur
        # le GIF.
        - obj:
            id: face_frame
            width: 420
            height: 420
            align: CENTER
            y: -40
            bg_opa: TRANSP
            border_width: 3
            border_color: 0x1C2331
            radius: 36
            pad_all: 0
            scrollable: false
            widgets:
              # ---> C'EST ICI QU'ON RÈGLE LA TAILLE DU VISAGE. <---
              # Garde ~61 % de la largeur du cadre pour rester fidèle au GIF.
              - obj:
                  id: face_panel
                  width: 256
                  height: 256
                  align: CENTER
                  y: -14
                  bg_opa: TRANSP
                  border_width: 0
                  pad_all: 0
                  scrollable: false

              # Bandeau de diagnostic du suivi caméra. Affiche le nombre de
              # visages vus et la direction du regard envoyée. Vide tant que
              # le debug est coupé.
              - label:
                  id: cam_debug
                  text: ""
                  text_color: 0x30D158
                  text_font: montserrat_18
                  align: TOP_MID
                  y: 12

              # Légende de l'émotion, dans le cadre comme dans le GIF.
              - label:
                  id: face_caption
                  text: ""
                  text_color: ${text_color_secondary}
                  text_font: montserrat_22
                  align: BOTTOM_MID
                  y: -18

        - button:
            id: btn_back_home
            width: 200
            height: 70
            align: BOTTOM_LEFT
            x: 40
            y: -20
            bg_color: 0x30363D
            radius: 35
            widgets:
              - label:
                  text: "Retour"
                  text_color: 0xFFFFFF
                  text_font: montserrat_22
                  align: CENTER
            on_click:
              - lvgl.page.show:
                  id: page_home
                  animation: MOVE_RIGHT
                  time: 250ms

        # Fait défiler les 17 expressions pour juger le rendu.
        - button:
            id: btn_demo
            width: 260
            height: 70
            align: BOTTOM_RIGHT
            x: -40
            y: -20
            checkable: true
            bg_color: 0x30363D
            radius: 35
            widgets:
              - label:
                  text: "Démo expressions"
                  text_color: 0xFFFFFF
                  text_font: montserrat_22
                  align: CENTER
            on_click:
              - switch.toggle: face_demo

# =============================================================================
# LE VISAGE KAWAII
# =============================================================================
# parent_id accepte l'id d'un widget (ici face_panel) ou directement l'id d'une
# page. Le visage est créé DANS ce widget — plus jamais sur la page affichée au
# démarrage.
lvgl_kawaii_face:
  id: face
  parent_id: face_panel
  initial_emotion: neutral
  animation_speed: 30ms
  blink_interval: 3000ms
  auto_blink: true
  smooth: true
  response_fallback: speaking
  # Optionnel : la couleur est déduite du fond de la page si on l'omet.
  # bg_color: ${background_color}

# =============================================================================
# VIE AUTONOME — le visage vit même quand on ne lui parle pas
# =============================================================================
# Tempérament « expressif » : une expression toutes les ~10 s sur toute la
# palette. Tout est mis en veille dès qu'une conversation démarre (va_busy),
# pour ne jamais écraser la réaction à l'assistant vocal.
#
# Pour calmer le visage : allonge l'interval, ou réduis la liste IDLE_SET.
# =============================================================================
# RYTHME DE LA JOURNÉE
# =============================================================================
time:
  - platform: homeassistant
    id: ha_time
    on_time:
      - hours: 7
        minutes: 0
        then:
          - lvgl_kawaii_face.set_emotion: { id: face, emotion: happy }
      - hours: 13
        minutes: 0
        then:
          - lvgl_kawaii_face.set_emotion: { id: face, emotion: cool }
      - hours: 22
        minutes: 30
        then:
          - lvgl_kawaii_face.set_emotion: { id: face, emotion: sleepy }
      - hours: 0
        minutes: 0
        then:
          # La nuit, on coupe la vie autonome : le visage dort vraiment.
          - lambda: id(idle_life).stop_poller();
      - hours: 7
        minutes: 1
        then:
          - lambda: id(idle_life).start_poller();

# =============================================================================
# RÉACTIONS À LA MAISON
# =============================================================================
# Remplace les entity_id par les tiens. Chaque bloc est indépendant : supprime
# ceux qui ne te servent pas.
binary_sensor:
  # Quelqu'un entre dans la pièce -> le visage s'illumine
  - platform: homeassistant
    id: ha_presence
    entity_id: binary_sensor.presence_salon   # <-- À ADAPTER
    on_press:
      then:
        - lambda: id(last_interaction) = millis();
        - lvgl_kawaii_face.set_emotion: { id: face, emotion: excited }
        - lvgl.page.show: { id: page_face, animation: MOVE_LEFT, time: 250ms }

  # Porte / sonnette -> surpris
  - platform: homeassistant
    id: ha_doorbell
    entity_id: binary_sensor.sonnette         # <-- À ADAPTER
    on_press:
      then:
        - lvgl_kawaii_face.set_emotion: { id: face, emotion: surprised }

text_sensor:
  # Alarme : armée -> working_hard, déclenchée -> angry
  - platform: homeassistant
    id: ha_alarm
    entity_id: ${alarm_panel_entity}
    on_value:
      then:
        - lvgl_kawaii_face.set_emotion:
            id: face
            emotion: !lambda |-
              if (x == "triggered") return "angry";
              if (x == "armed_away" || x == "armed_night") return "working_hard";
              if (x == "arming" || x == "pending") return "worried";
              return "neutral";

  # Météo : le visage prend le temps qu'il fait
  - platform: homeassistant
    id: ha_weather
    entity_id: ${weather_entity}
    on_value:
      then:
        - lvgl_kawaii_face.set_emotion:
            id: face
            emotion: !lambda |-
              if (x == "sunny" || x == "clear-night") return "cool";
              if (x == "rainy" || x == "pouring") return "sad";
              if (x == "lightning" || x == "lightning-rainy") return "surprised";
              if (x == "snowy") return "excited";
              return "neutral";
